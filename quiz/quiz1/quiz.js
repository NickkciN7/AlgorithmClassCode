"use strict";
/**
 * Q5. Given the following piece of pseudocode, write the corresponding Java code. Only write the included segment, not the entire algorithm.

 * algorithm findIndex
 *  input: array of int arr, int target x
 *  output: index of x in arr , -1 if not found
 *
 * for each index i and corresponding element e in array
 */
function findIndex(arr, x) {
    for (let i = 0; i < arr.length; i++) {
        const currentNumber = arr[i];
    }
}
